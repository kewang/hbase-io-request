# hbase-io-request

```bash
java -jar hbase-io-request.jar 10.1.18.167:60030 t inc y output.csv

# Please input HBase Region Server URL (e.g. 10.1.18.168:60030), sort field (e.g. 'w' or 'r' or 't'), sort direction (e.g. 'inc' or 'desc'), merge (e.g. 'y' or 'n'), output filename
```
